<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <link rel="stylesheet" href="./css/style.css" />
    <title>BlogWise</title>
  </head>
  <body>
    <!-- nav -->
    <%-include('partials/navbar.ejs') %>
    <!-- header -->
    <header>
      <div class="header-content">
        <h1>Exploring Horizons</h1>
        <p>
          "There is no greater agony than bearing an untold story inside you." - Maya Angelou
        </p>
        <a href="#">
          <span>Create New Post</span>
        </a>
      </div>
    </header>
    <!-- Latest post title -->

    <div class="latest-post-title">
      <!-- Icon -->
      <div class="icon">
        <i class="fas fa-book-open"></i>
      </div>
      <h1>Latest Posts (<%=posts.length%>)</h1>
      <p>
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam,
        voluptates.
      </p>
    </div>

    <% if(error) { %>
      <small class="error">
        <i class="fas fa-exclamation-circle"></i>
        <%=error%>
      </small>
    <% }%>

    <!-- content -->
    <section class="container-flex">
      <% if(posts?.length <= 0) { %>
        <h1>There are no posts</h1>
      <% } else { %>
        <% posts?.forEach(post => { %>
          <article class="post-content">
            <img src="<%=post?.image%>" alt="" class="post-image" />
            <p class="post-info">
              <span class="post-author">
                <i class="fas fa-user"></i>
                <a href="#"><%=post?.user?.fullname%></a>
              </span>
              <span class="post-date">
                <i class="far fa-calendar-alt"></i>
                <time datetime="<%=post?.createdAt%>"><%=post?.createdAt.toLocaleDateString()%></time>
              </span>
            </p><h1 class="post-title"><%=post?.title%></h1>
            <p class="post-body">
              <%=truncatePost(post?.description)%>
            </p>
            <!-- category -->
            <div class="post-category">
              <span>Category: <%=post?.category%></span>
            </div>
            <a href="/api/v1/posts/<%=post?._id%>" class="post-read-more">Continue Reading...</a>
          </article>
        <% }) %>
      <% }%>

        <!-- footer -->
        <%-include('partials/footer.ejs') %>
      
    </section>
    
  </body>
</html>
